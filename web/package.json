{
  "name": "flam-edge-detection-viewer-web",
  "version": "1.0.0",
  "description": "Web viewer for Flam Edge Detection processed frames",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "npm run build && npx http-server public -p 3000",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build",
    "sync": "node sync-frames.js",
    "sync:watch": "node sync-frames.js --watch"
  },
  "keywords": ["typescript", "canvas", "image-processing", "flam"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.7.0",
    "typescript": "^5.9.3"
  }
}
